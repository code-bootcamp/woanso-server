input {
    jdbc {
        jdbc_driver_library => '/usr/share/logstash/mysql-connector-java-8.0.28.jar' #자바 통해서 연결해야함
        jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
        jdbc_connection_string => "jdbc:mysql://my-database:3306/woanso"
        jdbc_user => "root"
        jdbc_password => "root"
        schedule => "* * * * *"  #리프레시 간격, 이대로 하면 매 분 리프레시함
        use_column_value => true  #이하 column의 value를 사용하겠다
        tracking_column => "updatedat"
        tracking_column_type => "timestamp"
        last_run_metadata_path => "./data.txt"  #저장되는 텍스트 파일
        statement => "select id, email, updatedat from user where updatedat > :sql_last_value order by updatedat asc" #asc는 오름차순 내림차순은 desc
    }
}


output {
    elasticsearch {
        hosts => "elasticsearch:9200"
        index => "myuser"        
    }


}